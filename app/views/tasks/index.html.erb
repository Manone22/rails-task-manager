<h1>My Tasks</h1>

  <ul>
    <% @tasks.each do |task| %>
    <%= task.completed ? "✓" : "𐄂" %>
    <%= link_to task.title, task_path(task) %>
    <%= link_to "Delete #{task.title}", task_path(task),
            method: :delete,
            data: { confirm: "Are you sure?" }%>
    <br>
    <% end %>
  </ul>

<h2><%= link_to '+ Add a new task', new_task_path %></h2>
